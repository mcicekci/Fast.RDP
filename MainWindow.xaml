<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FastRDP.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Üst Bar -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              Padding="16,12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo ve Başlık -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE968;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                <TextBlock Text="FastRDP" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Arama Kutusu -->
            <AutoSuggestBox Grid.Column="1" 
                            x:Name="SearchBox"
                            PlaceholderText="Profil, host veya etiket ara..."
                            QueryIcon="Find"
                            Margin="24,0"
                            VerticalAlignment="Center"
                            TextChanged="OnSearchTextChanged"/>

            <!-- Ayarlar -->
            <Button Grid.Column="2" 
                    Margin="0,0,8,0"
                    ToolTipService.ToolTip="Ayarlar"
                    Click="OnSettingsClick">
                <FontIcon Glyph="&#xE713;" FontSize="16"/>
            </Button>

            <!-- Tema Değiştir -->
            <Button Grid.Column="3" 
                    Margin="0,0,8,0"
                    ToolTipService.ToolTip="Tema Değiştir"
                    Click="OnThemeButtonClick">
                <FontIcon Glyph="&#xE771;" FontSize="16"/>
            </Button>

            <!-- Yenile -->
            <Button Grid.Column="4"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="OnRefreshClick"
                    ToolTipService.ToolTip="Yenile">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    <TextBlock Text="Yenile"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Ana İçerik -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Sol Panel - Filtreler -->
            <Grid Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
                <ScrollViewer>
                    <StackPanel Padding="16" Spacing="8">
                        <TextBlock Text="FİLTRELER" FontSize="12" FontWeight="SemiBold" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,8"/>

                        <RadioButton Content="Tümü" GroupName="Filter" IsChecked="True" 
                                     Click="OnFilterChanged" Tag="all"/>
                        
                        <RadioButton GroupName="Filter" Click="OnFilterChanged" Tag="favorites">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE735;" FontSize="14"/>
                                <TextBlock Text="Favoriler"/>
                            </StackPanel>
                        </RadioButton>
                        
                        <RadioButton GroupName="Filter" Click="OnFilterChanged" Tag="recent">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE823;" FontSize="14"/>
                                <TextBlock Text="Son Kullanılanlar"/>
                            </StackPanel>
                        </RadioButton>

                        <MenuFlyoutSeparator Margin="0,12"/>

                        <Button Click="OnCreateNewProfileClick"
                                Style="{StaticResource AccentButtonStyle}"
                                HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE710;" FontSize="14"/>
                                <TextBlock Text="Yeni Profil"/>
                            </StackPanel>
                        </Button>

                        <Button Click="OnBackupClick"
                                HorizontalAlignment="Stretch"
                                Margin="0,4,0,0">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE777;" FontSize="14"/>
                                <TextBlock Text="Yedekle"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Orta Panel - Profil Listesi -->
            <Grid Grid.Column="1" Padding="16"
                  AllowDrop="True"
                  DragOver="OnDragOver"
                  Drop="OnDrop">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Liste Başlığı -->
                <TextBlock Grid.Row="0" Text="PROFILLER" FontSize="12" FontWeight="SemiBold" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,12"/>

                <!-- Profil Listesi -->
                <ListView Grid.Row="1" 
                          x:Name="ProfilesList"
                          SelectionMode="Single"
                          DoubleTapped="OnProfileDoubleTapped"
                          SelectionChanged="OnProfileSelectionChanged"/>

                <!-- Drag & Drop Overlay -->
                <Border x:Name="DragDropOverlay"
                        Grid.Row="0" Grid.RowSpan="2"
                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                        Opacity="0"
                        IsHitTestVisible="False"
                        CornerRadius="8"
                        BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                        BorderThickness="3">
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                Spacing="16">
                        <FontIcon Glyph="&#xE896;" 
                                  FontSize="48" 
                                  Foreground="White"/>
                        <TextBlock Text="RDP dosyalarını buraya sürükleyin" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Sağ Panel - Detay -->
            <Grid Grid.Column="2" x:Name="DetailPanel" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1,0,0,0"
                  Padding="16">
                <ScrollViewer>
                    <StackPanel x:Name="ProfileDetails" Spacing="16" Visibility="Collapsed">
                        <TextBlock Text="PROFİL DETAYI" FontSize="12" FontWeight="SemiBold" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <!-- Profil Bilgileri -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="Bağlantı Adı:" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock x:Name="ProfileName" FontSize="16" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Host:" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock x:Name="ProfileHost" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Kullanıcı Adı:" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock x:Name="ProfileUsername" FontSize="14"/>
                        </StackPanel>

                        <MenuFlyoutSeparator/>

                        <!-- Aksiyonlar -->
                        <Button Content="Bağlan" 
                                Click="OnConnectClick"
                                Style="{StaticResource AccentButtonStyle}"
                                HorizontalAlignment="Stretch">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8A7;" FontSize="16"/>
                                        <TextBlock Text="Bağlan" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Düzenle" 
                                Click="OnEditClick"
                                HorizontalAlignment="Stretch">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                                        <TextBlock Text="Düzenle"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Sil" 
                                Click="OnDeleteClick"
                                HorizontalAlignment="Stretch"
                                Foreground="Red">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                                        <TextBlock Text="Sil"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Alt Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              Padding="16,8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="StatusText" VerticalAlignment="Center" FontSize="13">
                Toplam Profil: 0
            </TextBlock>

            <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="12" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                FastRDP v1.0
            </TextBlock>
        </Grid>
    </Grid>
</Window>
