<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="FastRDP.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="FastRDP"
    Width="1200"
    Height="700">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Üst Bar -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              Padding="16,12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Logo ve Başlık -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE968;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                <TextBlock Text="FastRDP" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Arama Kutusu -->
            <AutoSuggestBox Grid.Column="1" 
                            PlaceholderText="Profil, host veya etiket ara..."
                            QueryIcon="Find"
                            Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            Margin="24,0"
                            VerticalAlignment="Center"/>

            <!-- Tema Değiştir -->
            <Button Grid.Column="2" 
                    Style="{StaticResource SubtleButtonStyle}"
                    ToolTipService.ToolTip="Tema Değiştir"
                    Margin="0,0,8,0"
                    Click="OnThemeButtonClick">
                <FontIcon Glyph="&#xE771;" FontSize="16"/>
            </Button>

            <!-- Yenile -->
            <Button Grid.Column="3" 
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.RefreshCommand}"
                    ToolTipService.ToolTip="Yenile">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    <TextBlock Text="Yenile"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Ana İçerik -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Sol Panel - Filtreler -->
            <Grid Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
                <ScrollViewer>
                    <StackPanel Padding="16" Spacing="8">
                        <TextBlock Text="FİLTRELER" FontSize="12" FontWeight="SemiBold" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,8"/>

                        <RadioButton Content="Tümü" GroupName="Filter" IsChecked="True" 
                                     Click="OnFilterChanged" Tag="all"/>
                        <RadioButton Content="Favoriler" GroupName="Filter" 
                                     Click="OnFilterChanged" Tag="favorites">
                            <RadioButton.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE735;" FontSize="14"/>
                                        <TextBlock Text="Favoriler"/>
                                    </StackPanel>
                                </DataTemplate>
                            </RadioButton.ContentTemplate>
                        </RadioButton>
                        <RadioButton Content="Son Kullanılanlar" GroupName="Filter" 
                                     Click="OnFilterChanged" Tag="recent">
                            <RadioButton.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE823;" FontSize="14"/>
                                        <TextBlock Text="Son Kullanılanlar"/>
                                    </StackPanel>
                                </DataTemplate>
                            </RadioButton.ContentTemplate>
                        </RadioButton>

                        <MenuFlyoutSeparator Margin="0,12"/>

                        <Button Content="Yeni Profil" 
                                Command="{x:Bind ViewModel.CreateNewProfileCommand}"
                                Style="{StaticResource AccentButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Click="OnCreateNewProfileClick">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE710;" FontSize="14"/>
                                        <TextBlock Text="Yeni Profil"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Yedekle" 
                                Command="{x:Bind ViewModel.BackupProfilesCommand}"
                                HorizontalAlignment="Stretch"
                                Margin="0,4,0,0">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE777;" FontSize="14"/>
                                        <TextBlock Text="Yedekle"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Orta Panel - Profil Listesi -->
            <Grid Grid.Column="1" Padding="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Liste Başlığı -->
                <TextBlock Grid.Row="0" Text="PROFILLER" FontSize="12" FontWeight="SemiBold" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,12"/>

                <!-- Profil Listesi -->
                <ListView Grid.Row="1" 
                          ItemsSource="{x:Bind ViewModel.FilteredProfiles, Mode=OneWay}"
                          SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}"
                          SelectionMode="Single"
                          DoubleTapped="OnProfileDoubleTapped">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:RdpProfile">
                            <Grid Padding="12" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- İkon -->
                                <FontIcon Grid.Column="0" Glyph="&#xE968;" FontSize="32" 
                                          Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>

                                <!-- Bilgiler -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="15"/>
                                    <TextBlock Text="{x:Bind Host}" 
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                               FontSize="13"/>
                                    <TextBlock Text="{x:Bind Username}" 
                                               Foreground="{ThemeResource TextFillColorTertiaryBrush}" 
                                               FontSize="12" Margin="0,2,0,0"/>
                                </StackPanel>

                                <!-- Favori İkonu -->
                                <FontIcon Grid.Column="2" Glyph="&#xE735;" FontSize="16" 
                                          Foreground="{ThemeResource SystemFillColorAttentionBrush}"
                                          Visibility="{x:Bind Favorite}"/>

                                <!-- Sağ Tık Menüsü -->
                                <Grid.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="Bağlan" Icon="Link">
                                            <MenuFlyoutItem.Command>
                                                <Binding Path="ConnectCommand" Source="{StaticResource ViewModel}"/>
                                            </MenuFlyoutItem.Command>
                                            <MenuFlyoutItem.CommandParameter>
                                                <Binding/>
                                            </MenuFlyoutItem.CommandParameter>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Düzenle" Icon="Edit">
                                            <MenuFlyoutItem.Command>
                                                <Binding Path="EditProfileCommand" Source="{StaticResource ViewModel}"/>
                                            </MenuFlyoutItem.Command>
                                            <MenuFlyoutItem.CommandParameter>
                                                <Binding/>
                                            </MenuFlyoutItem.CommandParameter>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutSeparator/>
                                        <MenuFlyoutItem Text="Favorilere Ekle/Çıkar">
                                            <MenuFlyoutItem.Icon>
                                                <FontIcon Glyph="&#xE735;"/>
                                            </MenuFlyoutItem.Icon>
                                            <MenuFlyoutItem.Command>
                                                <Binding Path="ToggleFavoriteCommand" Source="{StaticResource ViewModel}"/>
                                            </MenuFlyoutItem.Command>
                                            <MenuFlyoutItem.CommandParameter>
                                                <Binding/>
                                            </MenuFlyoutItem.CommandParameter>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutItem Text="Klasörde Aç" Icon="Folder">
                                            <MenuFlyoutItem.Command>
                                                <Binding Path="OpenFolderCommand" Source="{StaticResource ViewModel}"/>
                                            </MenuFlyoutItem.Command>
                                            <MenuFlyoutItem.CommandParameter>
                                                <Binding/>
                                            </MenuFlyoutItem.CommandParameter>
                                        </MenuFlyoutItem>
                                        <MenuFlyoutSeparator/>
                                        <MenuFlyoutItem Text="Sil" Icon="Delete" Foreground="Red">
                                            <MenuFlyoutItem.Command>
                                                <Binding Path="DeleteProfileCommand" Source="{StaticResource ViewModel}"/>
                                            </MenuFlyoutItem.Command>
                                            <MenuFlyoutItem.CommandParameter>
                                                <Binding/>
                                            </MenuFlyoutItem.CommandParameter>
                                        </MenuFlyoutItem>
                                    </MenuFlyout>
                                </Grid.ContextFlyout>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Sağ Panel - Detay -->
            <Grid Grid.Column="2" Background="{ThemeResource LayerFillColorDefaultBrush}" 
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1,0,0,0"
                  Padding="16">
                <ScrollViewer Visibility="{x:Bind ViewModel.SelectedProfile, Mode=OneWay, Converter={StaticResource NullToVisibilityConverter}}">
                    <StackPanel Spacing="16">
                        <TextBlock Text="PROFİL DETAYI" FontSize="12" FontWeight="SemiBold" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <!-- Profil Bilgileri -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="Bağlantı Adı:" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.SelectedProfile.Name, Mode=OneWay}" 
                                       FontSize="16" FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Host:" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.SelectedProfile.Host, Mode=OneWay}" FontSize="14"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Kullanıcı Adı:" FontSize="12" 
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.SelectedProfile.Username, Mode=OneWay}" FontSize="14"/>
                        </StackPanel>

                        <MenuFlyoutSeparator/>

                        <!-- Aksiyonlar -->
                        <Button Content="Bağlan" 
                                Command="{x:Bind ViewModel.ConnectCommand}"
                                CommandParameter="{x:Bind ViewModel.SelectedProfile, Mode=OneWay}"
                                Style="{StaticResource AccentButtonStyle}"
                                HorizontalAlignment="Stretch">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE8A7;" FontSize="16"/>
                                        <TextBlock Text="Bağlan" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Düzenle" 
                                Command="{x:Bind ViewModel.EditProfileCommand}"
                                CommandParameter="{x:Bind ViewModel.SelectedProfile, Mode=OneWay}"
                                HorizontalAlignment="Stretch">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                                        <TextBlock Text="Düzenle"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>

                        <Button Content="Sil" 
                                Command="{x:Bind ViewModel.DeleteProfileCommand}"
                                CommandParameter="{x:Bind ViewModel.SelectedProfile, Mode=OneWay}"
                                HorizontalAlignment="Stretch"
                                Foreground="Red">
                            <Button.ContentTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                                        <TextBlock Text="Sil"/>
                                    </StackPanel>
                                </DataTemplate>
                            </Button.ContentTemplate>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Alt Bar -->
        <Grid Grid.Row="2" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              Padding="16,8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontSize="13">
                <Run Text="Toplam Profil:"/>
                <Run Text="{x:Bind ViewModel.TotalProfileCount, Mode=OneWay}" FontWeight="SemiBold"/>
            </TextBlock>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="16">
                <TextBlock VerticalAlignment="Center" FontSize="12" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run Text="FastRDP v1.0"/>
                </TextBlock>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

